{
  "name": "kilimolink-platform-final",
  "private": true,
  "description": "KilimoLink is a hyperlocal urban food resilience marketplace connecting city consumers directly with local farmers.",
  "author": "KilimoLink",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/geraldkombo/KilimoLink.git"
  },
  "homepage": "https://github.com/geraldkombo/KilimoLink",
  "keywords": [
    "KilimoLink",
    "marketplace",
    "food security",
    "climate resilience",
    "agritech",
    "Kenya",
    "urban farming",
    "carbon tracking"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "cd backend && npm run dev",
    "build": "cd backend && npm install --legacy-peer-deps && npx prisma generate && npm run build",
    "start": "cd backend && npx prisma db push; node dist/main.js",
    "lint": "cd backend && npm run lint",
    "test": "cd backend && npm test"
  }
}
